services:
  html2rss:
    build: .
    container_name: html2rss
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - VERIFICATION_CODE=test # 建议在生产环境修改此验证码
    # 如果需要修改启动参数，可以取消注释下面这行
    # command: ["--port=3000", "--verification-code=your_code"]
